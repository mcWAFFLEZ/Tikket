{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commints --yes",
    "diff": "lerna diff"
  }
}